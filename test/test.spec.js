import 'babel-polyfill';
import {expect}                   from 'chai';
import {describe, beforeEach, it} from 'mocha';

import obj from '../src/resources';

describe('Manifest', () => {

	describe('resources module', () => {

		it("contains the right classes", () => {

			// expect(obj).to.contain();

		});


	});


});
